{
  "name": "botmaker-webhook-auth",
  "version": "0.1.0",
  "description": "Express middleware compartilhado de bearer-token auth + rate limiting para webhooks/endpoints do ecossistema Botmaker",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "express": "^4.19.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
